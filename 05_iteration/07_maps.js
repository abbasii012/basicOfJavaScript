// const newNumber = [1,2,3,4,5,6,7]
// const myNum= newNumber.map((num)=>num*10).map((num)=>num+10).filter((num)=>num>=40)
// console.log(myNum);


//   filter give us true and flase values
// map is use to perform operatoion on array
// both filter and map return the values but foreach do not return values
